import { describe, it } from "node:test"

describe("Generator (placeholder)", () => {
    it.todo("emits readable JavaScript for statements")
    it.todo("emits JavaScript for expressions and calls")
    it.todo("handles name mangling for reserved identifiers")
})
